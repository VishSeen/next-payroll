import { Roboto, Poppins } from 'next/font/google';

export const poppins = Poppins({
    subsets: ['latin'],
    weight: ['400', '500']
});


export const roboto = Roboto({
    subsets: ['latin'],
    weight: ['400', '500']
});